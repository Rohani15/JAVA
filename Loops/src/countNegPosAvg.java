

	import java.util.Scanner;

	public class countNegPosAvg 
	{
/*
		public static void main(String[] args) 
		{ 
			int pos = 0;
			int neg=0;
		    int number;
		    int total =0, count=0; 

			
			System.out.println("Enter an int, the input ends if it is 0: ");
			Scanner input = new Scanner(System.in);
		    
		    while((number = input.nextInt()) != 0)
		    {
				total += number; //added all the inputed numbers
				count++; //counting the number of inputted numbers
				
		    	if(number > 0) pos++; 
		    	if(number < 0) neg++; 

		    }	
		    float avg = (total / (float) count); //totaling the average
		    System.out.println("The number of positives is " + pos);
		    System.out.println("The number of Negatives is " + neg);
		    System.out.println("The total is " + total);
		    System.out.println("The average is " + avg);




			
		}

	}
*/
}
